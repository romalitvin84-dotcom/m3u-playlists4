
name: Тест обновления плейлиста

on:
  workflow_dispatch:  # запуск вручную

jobs:
  update-playlist:
    runs-on: ubuntu-latest
    
    steps:
      - name: Забрать файлы
        uses: actions/checkout@v4

      - name: Подготовить Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Запустить программу
        run: |
          echo "Проверка: скрипт работает"  # Тестовый вывод, чтобы убедиться, что процесс запускается
